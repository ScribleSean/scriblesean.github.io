import resume from "./resume.json";

// The website, Files app and downloadable resume share this curated source.
export const { contact, education, projects, experience, resumeExperience, skills } = resume;
