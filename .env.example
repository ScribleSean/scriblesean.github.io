# A public AJAX form endpoint, for example FormSubmit's activated random endpoint
# with /ajax/ before its identifier. The destination should be sean.arackal@gmail.com.
# Leave blank during local design work: the Messages UI will offer an email fallback
# and will never show a false success state.
NEXT_PUBLIC_CONTACT_ENDPOINT=
